interface:
  display_name: "Judge AI Cost Hack"
  short_description: "Run entries and publish the cost leaderboard"
  default_prompt: "Use $judge-ai-cost-hack to run these repositories and publish the leaderboard."
