[
  {
    "id": "public-auth-delete",
    "title": "Cross-tenant project deletion",
    "brief": "Add a project deletion endpoint for the settings screen.",
    "context": [
      {
        "kind": "issue",
        "content": "Users should be able to delete projects they own from Settings."
      },
      {
        "kind": "diff",
        "path": "api/delete_project.py",
        "content": "def delete_project(request, project_id):\n    user = require_user(request)\n    project = db.projects.get(project_id)\n    db.projects.delete(project.id)\n    return {\"deleted\": True}\n"
      },
      {
        "kind": "test",
        "path": "tests/test_delete_project.py",
        "content": "def test_owner_can_delete(client, owned_project):\n    assert client.delete(f'/projects/{owned_project.id}').status_code == 200\n"
      }
    ],
    "rubric": {
      "risk": "high",
      "next_action": "block",
      "pass_score": 80,
      "must_find": ["missing-owner-check"],
      "required_test_terms": ["cross-tenant", "403"],
      "required_findings": [
        {
          "id": "missing-owner-check",
          "category": "authorization",
          "file": "api/delete_project.py",
          "evidence_anchor": "without an owner check"
        }
      ]
    }
  },
  {
    "id": "public-payment-idempotency",
    "title": "Retried payment webhook",
    "brief": "Handle payment-provider webhooks and record successful charges.",
    "context": [
      {
        "kind": "issue",
        "content": "The payment provider retries a webhook when it does not receive a response within five seconds."
      },
      {
        "kind": "diff",
        "path": "payments/webhook.py",
        "content": "def payment_succeeded(event):\n    invoice = invoices.get(event.invoice_id)\n    ledger.capture(invoice.customer_id, event.amount)\n    processed_events.insert(event.id)\n    return {\"ok\": True}\n"
      },
      {
        "kind": "test",
        "path": "tests/test_webhook.py",
        "content": "def test_payment_succeeded(invoice, event):\n    assert payment_succeeded(event) == {\"ok\": True}\n    assert ledger.charge_count(invoice.customer_id) == 1\n"
      }
    ],
    "rubric": {
      "risk": "high",
      "next_action": "block",
      "pass_score": 80,
      "must_find": ["charge-before-idempotency-check"],
      "required_test_terms": ["same event", "one charge"],
      "required_findings": [
        {
          "id": "charge-before-idempotency-check",
          "category": "idempotency",
          "file": "payments/webhook.py",
          "evidence_anchor": "capture happens before the event is recorded"
        }
      ]
    }
  }
]
